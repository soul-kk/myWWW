export default function TabLabel({ children }: { children: React.ReactNode }) {
  return (
    <p className="font-open-sans text-[94px] font-semibold leading-[1.1] tracking-tighter">
      {children}
    </p>
  );
}
